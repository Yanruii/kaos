from . import Vector3D

def get_sattlite_pos_vel( satillite_info, time )
    """  Satellite info is a number to be indexed from the DB.
        time is in posix time.
        needs to return ( Vector3D, Vector3D ) <- ( pos, vel ) at time
    """
    pass

